[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "netscan-homelab"
version = "0.1.0"
description = "Homelab network scanner, inventory and monitoring with Proxmox/TrueNAS integration"
readme = "README.md"
requires-python = ">=3.11"
license = "GPL-2.0-or-later"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "NetScan contributors" }]
keywords = ["network", "scanner", "homelab", "proxmox", "truenas", "monitoring"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Networking :: Monitoring",
]
dependencies = [
    # Version floors below are security floors, not just feature floors —
    # see CHANGELOG / security audit notes before lowering any of them.
    "scapy>=2.7.0",  # <2.7.0: GHSA-cq46-m9x9-j8w2 (unsafe pickle deserialization)
    "rich>=13.0.0",
    "typer>=0.12.0",
    "fastapi>=0.110.0",
    "starlette>=1.3.1",  # fastapi's own floor pulls in old, vulnerable starlette otherwise
    "uvicorn[standard]>=0.29.0",
    "sqlmodel>=0.0.16",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.2.0",
    "httpx>=0.27.0",
    "PyYAML>=6.0.1",
    "apprise>=1.7.0",
    "zeroconf>=0.149.17",  # <0.149.17: PYSEC-2026-3435/6/7/8/9
    "mac-vendor-lookup>=0.1.12",
    "cryptography>=49.0.0",  # <49.0.0: multiple PYSEC/GHSA advisories, see pip-audit
    "psutil>=5.9.0",
]

[project.optional-dependencies]
netifaces = ["netifaces>=0.11.0"]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.6.0",
    "mypy>=1.11.0",
    "respx>=0.21.0",
]

[project.scripts]
netscan = "netscan.cli:app"
netscan-server = "netscan.api.app:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 110
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
mypy_path = "src"
ignore_missing_imports = true
warn_unused_ignores = true
packages = ["netscan"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
asyncio_mode = "auto"

[tool.coverage.run]
source = ["src/netscan"]
omit = ["*/tests/*"]
